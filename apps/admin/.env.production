# Production build config (Vite inlines VITE_* at build time). The admin app holds
# NO secrets in its bundle — the admin key is entered at runtime and kept only in
# the browser's sessionStorage, sent per-request as the X-Admin-Key header.
VITE_API_URL=https://sprout-production-8da1.up.railway.app/api
